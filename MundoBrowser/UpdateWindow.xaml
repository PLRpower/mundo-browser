<Window x:Class="MundoBrowser.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MundoBrowser Updater" Height="250" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen">
    <Border Background="#1E1E1E" CornerRadius="10" BorderBrush="#333333" BorderThickness="1">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="MundoBrowser" Foreground="White" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30"/>
            <TextBlock x:Name="StatusText" Text="Vérification des mises à jour..." Foreground="#CCCCCC" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,15"/>
            <ProgressBar x:Name="UpdateProgress" Width="300" Height="6" Minimum="0" Maximum="100" Visibility="Collapsed" Foreground="#007ACC" Background="#333333" BorderThickness="0"/>
        </StackPanel>
    </Border>
</Window>
